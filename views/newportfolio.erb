<!DOCTYPE html>
<html lang="jp">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新規ポートフォリオ</title>
    <link rel="stylesheet" href="/css/newportfolio.css">
    <style>
        /* 追加: 画像選択時のスタイル */
        .image-item.selected {
            border: 2px solid blue; /* 選択された画像に青い枠線 */
            box-shadow: 0 0 10px rgba(0, 0, 255, 0.5); /* 影を追加 */
        }
    </style>
</head>
<body>
    <div class="container">
        <form action="/makeportfolio" method="post" onsubmit="return validateSelection()">
            <div class="instructions">
                <p>以下のデータがポートフォリオに登録されます</p>
            </div>
            <div class="image-flex">
                <% if @items.nil? %>
                    <p>ここには何もないよ</p>
                <% else %>
                    <% @items.each do |item| %>
                        <div class="image-item" data-value="<%= item.id %>">
                            <a href="#">
                                <img src="<%= item.image_url %>" alt="<%= item.name %>">
                                <p><%= item.name %></p>
                                <p>#<%= item.category %></p>
                                <p class="item-price"><%= item.price %></p>
                            </a>
                        </div>
                    <% end %>
                <% end %>
            </div>
            <!-- Hidden Inputs for Selected Images -->
            <input type="hidden" name="selected_image1" id="selected_image1">
            <input type="hidden" name="selected_image2" id="selected_image2">
            <br><br>
            <div class="header">
                <div class="color-picker">
                    <label for="color" class="p">推しカラーを選択<br></label>
                    <input type="color" name="color" value="#000000">
                </div><br>
                <input type="hidden" name="genre" value='<%= @genre %>'>
                <input type="hidden" name="total_price" value='<%= @total_price %>'>
                <input type="hidden" name="genre_price" value='<%= @genre_price %>'>
                <input type="hidden" name="genre_percent" value='<%= @genre_percent %>'>
                <button class="btn">ポートフォリオ作成！</button>
            </div>
        </form>
        <br><a href="/mypage/<%= session[:user] %>" class="btn back-btn">もどる</a>
</script>
    </body>
</html>
