<!DOCTYPE html>
<html lang="jp">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mypage-推し活ポートフォリオ</title>
</head>
<body>
    <form action="/signout" method="post">
        <button>ログアウト</button>
    </form>
    <h1>ログインできたよ</h1>
    <p>ようこそ<%= current_user.name %></p>
    <a href="/record">推し活を記録しよう</a>
    <div class="item-container">
        <h3>最近購入</h3><br>
        <% if @items.nil?%>
            <p>まだ登録されていないよ</p>
        <% else %>
            <% sorted_items = @items.order(created_at: :desc).limit(6) %>
            <% count = 0 %>
            <% sorted_items.each do |item| %>
        <div class="item">
            <a href="#">
                <img src="<%= item.image_url %>">
                <p><%= item.name %></p>
                <p>#<%= item.category%></p>
                <p class="item-price"><%= item.price %></p>
            </a>
        </div>
        <% count += 1 %>
        <% break if count >= 6 %>
        <% end %>
        <% end %>
</div>
</body>
</html>